import { Subscriber } from '../Subscriber';
import { rxSubscriber } from '../symbol/rxSubscriber';
export function toSubscriber(next, error, complete) {
    if (next && typeof next === 'object') {
        if (next instanceof Subscriber) {
            return next;
        }
        else if (typeof next[rxSubscriber] === 'function') {
            return next[rxSubscriber]();
        }
        else {
            return new Subscriber(next);
        }
    }
    return Subscriber.create(next, error, complete);
}
//# sourceMappingURL=toSubscriber.js.map